// EXPECTED OUTPUT:
// created
// hello from struct
struct Widget {
  val id: Int
}

fn Widget.create() -> Widget {
  println("created")
  Widget(1)
}

fn Widget.greet(ref self) -> String {
  return "hello from struct"
}

@entrypoint
fn main() {
  val w = Widget.create()
  println(w.greet())
}
