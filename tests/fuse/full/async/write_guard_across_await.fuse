// EXPECTED WARNING:
// warning: write guard held across await
//   --> write_guard_across_await.fuse:9:15

@entrypoint
async fn main() {
  @rank(1) val cache: Shared<Int> = Shared::<Int>.new(1)
  val guard = cache.write()
  val value = await "ok"
  println(value)
}
