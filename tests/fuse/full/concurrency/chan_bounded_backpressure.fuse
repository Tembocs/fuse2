// EXPECTED OUTPUT:
// blocked until receiver drains

@entrypoint
fn main() {
  val (tx, rx) = Chan::<Int>.bounded(1)
  tx.send(1)
  spawn { tx.send(2) }
  rx.recv()
  rx.recv()
  println("blocked until receiver drains")
}
