// EXPECTED OUTPUT:
// Some(1)
// Some(2)

import stdlib.core.map

@entrypoint
fn main() {
  var m = Map::<String, Int>.new()
  m.set("a", 1)
  m.set("b", 2)
  println(m.get("a"))
  println(m.get("b"))
}
