// EXPECTED OUTPUT:
// Some(1)
// Some(99)

import stdlib.core.map

@entrypoint
fn main() {
  var m = Map::<String, Int>.new()
  m.set("x", 1)
  println(m.get("x"))
  m.set("x", 99)
  println(m.get("x"))
}
