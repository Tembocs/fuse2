// EXPECTED OUTPUT:
// 42

interface Convertible {
  fn convert(ref self) -> String
}

data class Wrapper(val value: Int) implements Convertible

fn Wrapper.convert(ref self) -> String => f"{self.value}"

@entrypoint
fn main() {
  val w = Wrapper(42)
  println(w.convert())
}
