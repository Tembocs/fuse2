// EXPECTED OUTPUT:
// 3

import stdlib.core.list

fn defaultList<T>(item: T, n: Int) -> List<T> {
  var result: List<T> = []
  var i = 0
  while i < n {
    result.push(item)
    i = i + 1
  }
  result
}

@entrypoint
fn main() {
  val xs = defaultList(0, 3)
  println(xs.len())
}
