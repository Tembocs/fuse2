// EXPECTED OUTPUT:
// Some(10)
// None

fn findFirst<T>(xs: List<T>) -> Option<T> {
  if xs.len() > 0 {
    xs.get(0)
  } else {
    None
  }
}

@entrypoint
fn main() {
  println(findFirst([10, 20, 30]))
  val empty: List<Int> = []
  println(findFirst(empty))
}
