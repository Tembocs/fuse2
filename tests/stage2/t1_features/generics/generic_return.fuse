// EXPECTED OUTPUT:
// Some(42)
// None

fn tryGet<T>(xs: List<T>, fallback: T) -> T {
  if xs.len() > 0 { fallback } else { fallback }
}

@entrypoint
fn main() {
  val a: Option<Int> = Some(42)
  println(a)
  val b: Option<Int> = None
  println(b)
}
