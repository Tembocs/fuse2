// EXPECTED OUTPUT:
// 42
// hello

struct Box<T> {
  val item: T

  fn getItem(ref self) -> T {
    self.item
  }
}

@entrypoint
fn main() {
  val intBox = Box(42)
  println(intBox.getItem())
  val strBox = Box("hello")
  println(strBox.getItem())
}
