// EXPECTED OUTPUT:
// Some(abc)
// None

data class Item(val name: String)

@entrypoint
fn main() {
  val a: Option<Item> = Some(Item("abc"))
  println(a?.name)
  val b: Option<Item> = None
  println(b?.name)
}
