// EXPECTED ERROR:
// cannot use `f` after `move`

data class Foo(val x: Int)

fn consume(owned f: Foo) {
  println(f.x)
}

@entrypoint
fn main() {
  val f = Foo(1)
  var i = 0
  while i < 3 {
    consume(move f)
    i = i + 1
  }
}
