// EXPECTED OUTPUT:
// read: 42

fn readShared(ref s: Shared<Int>) -> Int {
  s.read()
}

@entrypoint
fn main() {
  @rank(1) val s: Shared<Int> = Shared::<Int>.new(42)
  val v = readShared(ref s)
  println(f"read: {v}")
}
