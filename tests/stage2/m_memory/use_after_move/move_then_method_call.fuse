// EXPECTED ERROR:
// cannot use `r` after `move`

data class Res(val name: String) implements Printable

fn consume(owned r: Res) { println(r.name) }

@entrypoint
fn main() {
  val r = Res("hello")
  consume(move r)
  println(r.toString())
}
