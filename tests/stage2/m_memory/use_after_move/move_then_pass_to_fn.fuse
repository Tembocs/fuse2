// EXPECTED ERROR:
// cannot use `r` after `move`

data class Res(val name: String)

fn consume(owned r: Res) { println(r.name) }
fn inspect(ref r: Res) { println(r.name) }

@entrypoint
fn main() {
  val r = Res("x")
  consume(move r)
  inspect(ref r)
}
