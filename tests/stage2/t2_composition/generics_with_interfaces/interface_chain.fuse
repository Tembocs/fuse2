// EXPECTED OUTPUT:
// Point(1, 2)
// Point { x: 1, y: 2 }

data class Point(val x: Int, val y: Int) implements Printable, Debuggable

@entrypoint
fn main() {
  val p = Point(1, 2)
  println(p.toString())
  println(p.debugString())
}
